{
  "mcpServers": {
    "github": {
      "command": "bash",
      "args": [
        "-c",
        "source .env && docker run -i --rm -e GITHUB_PERSONAL_ACCESS_TOKEN ghcr.io/github/github-mcp-server"
      ]
    }
  }
}
